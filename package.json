{
  "name": "ying",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:apps": "turbo run dev --filter=./apps/*",
    "build:admin": "turbo run build --filter=@ying/admin",
    "build:client": "turbo run build --filter=@ying/client",
    "build:server": "turbo run build --filter=@ying/server",
    "build:apps": "turbo run build --filter=./apps/*",
    "build:pkgs": "turbo run build --filter=./packages/*",
    "lint": "turbo run lint",
    "check": "turbo run check",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "turbo-prune": "turbo prune server --docker",
    "proxy": "ying-tunnel lhp",
    "prepare": "husky"
  },
  "packageManager": "pnpm@10.33.4",
  "devDependencies": {
    "@commitlint/cli": "21.2.2",
    "@commitlint/config-conventional": "21.2.2",
    "@swc/core": "1.16.1",
    "@types/node": "26.6.2",
    "@ying-tunnel/cli": "1.4.0",
    "husky": "9.1.7",
    "lint-staged": "17.4.1",
    "oxlint": "1.80.0",
    "prettier": "3.9.6",
    "tsup": "8.5.1",
    "turbo": "2.9.12",
    "typescript": "6.0.3"
  }
}
